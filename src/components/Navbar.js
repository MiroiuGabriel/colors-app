import './css/navbar.css';
const Navbar = () => {
	return (
		<div className="navbar">
			<h3 className="navbar-heading">Press the spacebar to generate color palettes</h3>
			<button>Add color</button>
		</div>
	);
};

export default Navbar;
